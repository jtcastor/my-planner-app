import { HomeScreen } from '../src/presentation/screens/HomeScreen';

// Index: Maps to root route, renders the home screen.
// Expo Router auto-discovers this.
export default function Index() {
  return <HomeScreen />; // Renders the main screen
}